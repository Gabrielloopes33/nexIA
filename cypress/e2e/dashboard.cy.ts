describe('Dashboard - NexIA Chat', () => {
  beforeEach(() => {
    // Substitua com credenciais de teste válidas
    // cy.login('seu-email@teste.com', 'sua-senha')
  })

  it.skip('deve acessar o dashboard após login', () => {
    // Descomente e ajuste após configurar credenciais de teste
    // cy.visit('/dashboard')
    // cy.url().should('include', '/dashboard')
    // cy.contains('Dashboard').should('be.visible')
  })

  it.skip('deve navegar para a página de contatos', () => {
    // Descomente após configurar login
    // cy.visit('/contatos')
    // cy.url().should('include', '/contatos')
    // cy.contains('Contatos').should('be.visible')
  })
})
